<!doctype html>
<html {{ site.language_attributes }}{% if debug %} data-debug="true"{% endif %}>
<head>
    <meta charset="{{ site.charset }}" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{ fn('wp_head') }}
